spring:
  application:
     name: 6_TestAPI

 cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: test-api
                uri: lb://TEST-API
                   predicate:
                      -Path = /test
